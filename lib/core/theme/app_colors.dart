import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const primary = Color(0xFF6C63FF);      // Modern Purple
  static const secondary = Color(0xFF32D74B);    // Neon Green
  
  // Background Colors
  static const background = Color(0xFF1A1A1A);    // Dark Background
  static const cardBackground = Color(0xFF2D2D2D);// Card Background
  
  // Gradient Colors
  static const gradientStart = Color(0xFF6C63FF); // Purple
  static const gradientEnd = Color(0xFF2196F3);   // Blue
  
  // Text Colors
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFB3B3B3);
  
  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFFF3B30);
  static const warning = Color(0xFFFFD60A);
}